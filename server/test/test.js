import Market from '../markets/Market'
import Promise from 'bluebird'

let permuation
createMarket(tokenA, tokenB) {
  return new Promise((resolve, reject) => {
    return Promise.delay(0).then(() => {
      return permutation = new Market()
    })
  })
}

createMarket()
